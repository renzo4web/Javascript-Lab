export const teams = {
  ferrari: {
    driver: 'Charles Leclerc',
    position: 1,
  },
  mercedes: {
    driver: 'Valteri Bottas',
    position: 2,
  },
  renault: {
    driver: 'Daniel Ricciardo',
    position: 4,
  },
};

// export const checkDriver = (id, callback) => {
//   const team = teams[id];
//   team ? callback(null, team) : callback(`Team With id:${id} not found`);
// };