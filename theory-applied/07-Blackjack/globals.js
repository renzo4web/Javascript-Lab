
  const btnTakeCard = document.getElementById('takeCard');
  const btnStop = document.getElementById('stop');
  const btnNewGame = document.getElementById('resetgame');
  const playerCards = document.querySelector('.container.player-cards');
  const machineCards = document.querySelector('.machine-cards');
  const playerScore = document.getElementById('playerScore');
  const machineScore = document.getElementById('machineScore');
  const playerLabel = document.querySelector('.player');
  const machineLabel = document.querySelector('.machine');
  const imgCards = document.querySelectorAll('.container');